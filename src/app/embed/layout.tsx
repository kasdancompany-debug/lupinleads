import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Lead Form — LUPIN LEADS",
};

export default function EmbedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
