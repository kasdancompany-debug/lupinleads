/** Shared primary CTA sizing — use on BookCallButton size="lg" */
export const CTA_PRIMARY_CLASS =
  "w-full sm:w-auto min-h-[52px] px-6 sm:px-8 text-base sm:text-[1.0625rem]";

/** Sticky mobile bar CTA */
export const CTA_STICKY_CLASS = "w-full min-h-[52px] text-base font-semibold";

/** Compact nav CTA (mobile header) */
export const CTA_NAV_CLASS = "min-h-[44px] text-xs sm:text-sm px-3 sm:px-4";
